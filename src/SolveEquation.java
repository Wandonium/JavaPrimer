public class SolveEquation {

    public static void main(String[] args) {
        // create a program for solving the equation
        // (a+b)square = ((a*a) + 2ab + (b*b));
        int a =
    }

    public int int_aPlus_bSquared(int a, int b) {
        int aSquared = a * a;
        int bSquared = b * b;
        int mid = 2 * a * b;
        return aSquared + bSquared + mid;
    }

    public int int_aPlus_bSquared(int a, int b) {
        int aSquared = a * a;
        int bSquared = b * b;
        int mid = 2 * a * b;
        return aSquared + bSquared + mid;
    }
}
